serenity {
  project.name = "Automation Exercises"
}

webdriver {
  driver = "chrome"
}

drivers {
  chrome {
    type = "chrome"
    driver_version = "latest"
    binary = "/usr/bin/google-chrome"
  }
}


drivers {
  chrome {
    driver_version = "latest"
  }
}
